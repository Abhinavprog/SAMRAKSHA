/** @type {import('next').NextConfig} */
const nextConfig = {
  // Font optimization is enabled by default in Next.js 16
  // We're using a simpler approach by using standard fonts
};

module.exports = nextConfig;
